_The immutable root-of-truth for one campaign. Written by the human principal before the campaign starts; every plan must trace to it, and drift is measured against it. Only the human may change it. Structure is normative per PRD §6.2 — keep all seven headings, in this order._

# Goal

<one line: the destination this campaign is finding its way to>

This IS the campaign's destination in the wayfinder sense — naming it is the first act of charting, and it fixes scope. Everything on the map is either on the way to it or ruled out of scope.

# Success criteria (measurable)

- <criterion 1: a number, a threshold, or a verifiable state — e.g. "cash-flows ≥ $X/mo">
- <criterion 2>
- <criterion 3, optional — prefer few and measurable over many and vague>

# Non-goals (explicitly excluded)

These seed the map's out-of-scope ledger. Work ruled beyond the destination never graduates back onto the frontier.

- <non-goal 1: something adjacent that this campaign will NOT do>
- <non-goal 2>

# Hard constraints

Constraints the orchestrator may never trade away, regardless of confidence.

- <constraint 1: e.g. budget ceiling, deadline, jurisdiction, tech stack>
- <constraint 2: e.g. "no irreversible action without human approval" — restate campaign-specific gates here>
- The deterministic verifier always wins over any LLM opinion.

# What "done" looks like

<a short concrete paragraph: the observable end-state. For a wayfinding campaign this is usually "a decision-complete spec, ratified by the human" — explicitly not execution. For an execution campaign, name the shipped artifact and its acceptance test.>

# Decision framework when something isn't specified

When an agent hits a question this file does not answer:

1. <default preference 1: e.g. "prefer the cheaper reversible option">
2. <default preference 2: e.g. "prefer the option that keeps more success criteria satisfiable">
3. If the question is precise and consequential → open a decision ticket on the map.
4. If it cannot yet be stated precisely → record it as fog.
5. If it is irreversible or moves money → stop and request human approval.

# What changes this file

Only the human principal. Amendments are logged events (the event log records who, when, and the diff). A redraw of the destination is not an amendment — it ends this campaign and starts a fresh effort with a new INTENT.

_Template: harness-reference v0.2.0 · see prd/ and WAYFINDER-DESIGN.md for rationale_
